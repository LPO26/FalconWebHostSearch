# FalconWebHostSearch
Local web app for searching CrowdStrike Falcon and Tenable from the browser: hosts by IP / hostname / OS / login user, plus per-host vulnerabilities, open ports, installed applications, and detections — and standalone threat-intel browsing with readable reports. It is the web sibling of the FalconHostSearch.py CLI and supports the same features.
